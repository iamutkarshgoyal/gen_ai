# gen_ai

Python | LLM Models | Streamlit

Run below commands to get started with this project:
git pull "https://github.com/iamutkarshgoyal/gen_ai.git"
cd gen_ai
pip install -r requirement.txt
streamlit run app.py